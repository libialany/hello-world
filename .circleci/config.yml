version: 2.1
jobs: 
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "I'M excited"
workflows:
  version: 2
  build: 
    jobs:
      - welcome/run
      - build
      
